#include "router.h"

namespace utils
{
  void showWindow(const views::BaseWidget& baseWidget, Route route)
  {
    switch (route) {
    case Route::SignIn:
       
      break;
    case Route::SignUp:
      break;
    case Route::Account:
      
      break;
    }
  }
}